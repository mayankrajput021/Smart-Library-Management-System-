package com.slms.model;

/**
 * Enumerates the roles supported by the system.
 * Used for role-based access control across the UI and service layers.
 */
public enum Role {
    ADMIN,
    LIBRARIAN,
    MEMBER
}
